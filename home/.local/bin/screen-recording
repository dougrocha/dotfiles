#!/bin/bash

# Only show indicator for active screen sharing (Discord/browser via portal)
# Don't show for replay buffer (always running in background)
if pw-dump | grep -q "xdph-streaming"; then
  echo '{"text": "󰻂", "tooltip": "Screen Sharing Active", "class": "active"}'
else
  echo '{"text": ""}'
fi

# vim: ft=sh

#!/bin/bash

if pgrep -f "^gpu-screen-recorder" >/dev/null; then
  pkill -f "^gpu-screen-recorder"
  notify-send "Recording Stopped" "Screen recording saved" -t 5000 -a "GPU Screen Recorder"
else
  start-recording screen
fi

# vim: ft=sh
